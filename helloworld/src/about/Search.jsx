import { React } from 'react'
import { Route, Switch, /* useLocation */ } from 'react-router-dom';

import './Searchs.scss';

const Search = (props) => {
    return(
        <div>
        </div>
    );
}

export default Search;